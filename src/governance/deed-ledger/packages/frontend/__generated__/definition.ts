import type { RuntimeCompositeDefinition } from '@composedb/types';

/**
 * Stub runtime composite definition.
 * Replace with the output of `pnpm deploy:models` once Ceramic models are deployed.
 */
const definition: RuntimeCompositeDefinition = {
  models: {},
  objects: {},
  enums: {},
  accountData: {},
};

export default definition;
